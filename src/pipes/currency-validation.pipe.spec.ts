import { CurrencyValidationPipe } from './currency-validation.pipe';

describe('CurrencyValidationPipe', () => {
  it('should be defined', () => {
    expect(new CurrencyValidationPipe()).toBeDefined();
  });
});
